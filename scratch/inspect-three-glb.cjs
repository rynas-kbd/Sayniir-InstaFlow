const fs = require('fs')
const THREE = require('three')

// We can inspect node transforms and bounds
const filePath = 'public/models/iphone-16.glb'
console.log('Inspecting THREE bounding box for iphone-16.glb...')
